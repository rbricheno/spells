" Bram Moolenaa forced me to write this file so it could have this line in it.
" https://github.com/vim/vim/issues/2841#issuecomment-396368637
set mouse-=a

" This is normally a systemwide default which is overridden if this file exists.
if has("syntax")
  syntax on
endif
